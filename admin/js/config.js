// Admin panel config - kept separate from the public frontend's config.js
// since the two are deployed independently.

// Change to your deployed backend URL once you deploy (e.g. Render URL).
const API_BASE_URL = "https://kamil-cafe.onrender.com/api";
const UPLOADS_BASE_URL = "https://kamil-cafe.onrender.com/uploads";

// The public website's URL, used for the "Back to website" / "View Live Site" links.
const FRONTEND_URL = "kamil-cafe.pages.dev";
