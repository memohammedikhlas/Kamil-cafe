// Change this to your deployed backend URL once you deploy (e.g. Render URL).
// While testing locally, keep it pointing at your local backend port.
const API_BASE_URL = "http://localhost:5000/api";
const UPLOADS_BASE_URL = "http://localhost:5000/uploads";
